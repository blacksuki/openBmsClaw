# openBattery 顶层技术架构（总纲 / hub）

> 最新更新：2026-07-21
> 架构状态：Accepted / 继续沿用
> 文档性质：架构总纲与导航中枢（hub）。战略定位、核心结论、项目边界见本文；详细内容已按主题拆分至 05–08（见下方导航）。
> 决策范围：openBattery 平台战略、MCU + SoC 双芯片协同架构、NFC(ST25DV)数据读出、软件分层边界、startup 阶段实施约束

## 1. 文档目的

本文用于明确 `openBattery` 的战略定位、核心架构决策、软件分层规则和当前实施路线。

它不是完整产品规格书，也不是某一块 PCB 的定板说明。它的作用是让后续计划、编码、采购、样片验证和文档更新都沿着同一条主线执行，避免 startup 阶段因为临时验证代码不断堆积成长期技术债。

本文的核心结论：

1. `openBattery` 继续采用 **MCU + 专用电源 SoC** 的双芯片协同架构，**辅以 NFC(ST25DV)作为数据读出窗口**(见 [05.technology_roadmap.md](05.technology_roadmap.md) ADR-003)。
2. SoC 负责高频电源转换、快充协议握手和基础硬件保护。
3. MCU 负责状态读取、策略调度、**数据可读(经 I²C 写入 NFC 标签供 tap 读出,而非设备端屏幕)**、日志记录和系统级安全管理。
4. 数据用途为**安全合规 + 监控(记录)**;用户 tap 读当前状态即可,**实时/隔空/远程监控/OTA 不在需求内(2026-07-18 锁定)**,BLE 降为 premium 备选。
5. 当前开发入口是 `STM32F103C8T6` 学习板和 `openBmsClaw/` 工程。
6. 当前量产候补优先评估 64KB Flash 档位 `F030`，但必须通过样片、资源和可靠性验证后才能进入量产参考设计;NFC 只用 I²C、无射频栈,放宽了对量产 MCU 的资源压力。

---

## 2. 文档导航（详细内容拆分位置）

本文（03）现为**架构总纲 / hub**，只保留战略定位、核心结论与项目边界。原 03 各章节的详细内容已按主题与 owner 拆分到以下文档；跟随旧引用（如 “§5.3” “§8” “ADR-002”）请按下表定位：

| 主题 | 现所在文档 | 对应原 03 章节 |
| --- | --- | --- |
| 架构决策记录（ADR-001/002/003）、实施路线与阶段门（Phase 0–4）、近期建议动作 | [05.technology_roadmap.md](05.technology_roadmap.md) | §2 / §8 / §10 |
| 软件分层与依赖矩阵、SoC 抽象层设计、安全快慢路径、配置与资源 Profile、技术债预防 | [06.software_architecture.md](06.software_architecture.md) | §4 / §5.1-5.2 / §6 / §7 / §9 |
| 硬件系统框图、power tree、关键器件、硬件→软件接口划分、待确认项 | [07.hardware_architecture.md](07.hardware_architecture.md) | 由 ADR 事实 + 硬件模板实例化 |
| SoC 公共 API 规范（`soc_api.h` 当前代码事实、契约、Phase 1 演进） | [08.api_spec.md](08.api_spec.md) | §5.3（已修正与代码的漂移） |

> 拆分说明（2026-07-21）：原 03 单文档同时承载战略、软件、硬件、API 四类内容，篇幅过长且职责混杂、owner 不清。本次按主题拆分为 05–08，03 收敛为总纲。原 §5.3 列出的接口签名（`soc_get_voltage_mv` 等）为**规划中的理想接口**，与当前 `soc_api.h` 代码不一致；已在 [08.api_spec.md](08.api_spec.md) 中以“当前代码事实 vs 规划中”三态修正，理想签名保留为演进参考。

---

## 3. 当前项目边界

### 3.1 仓库边界

```text
openBattery/
├── 0_System/          系统文档、架构规范、开发准备
├── 1_Plan/            周计划和任务拆解
├── 2_Action/          实施记录和验证记录
├── 90_documents/      外部文档、立项资料与参考教程
├── 91.reference/      外部资料归档
└── openBmsClaw/       当前 STM32 工程事实来源
```

边界规则：

- `openBmsClaw/` 是当前代码事实来源。
- `90_documents/` 是外部文档与立项/参考资料，不能直接当作当前实现或代码事实。
- `91.reference/` 是外部资料归档，不默认等于项目决策。
- `build/` 是生成物，不作为长期维护源码。

### 3.2 产品边界

`openBattery` 的目标不是先做满一台移动电源的所有功能，而是先形成可复用的开放平台骨架。

当前优先级：

1. 脑体协同架构正确。
2. SoC 接口抽象稳定。
3. MCU bring-up 可观测。
4. F030 量产候补约束明确。
5. 服务层策略能逐步验证。
